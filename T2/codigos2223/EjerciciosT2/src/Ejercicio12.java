import java.util.Scanner;

public class Ejercicio12 {

    public static void main(String[] args) {
        /*Scanner lecturaTeclado = new Scanner(System.in);
        int numeroUno, numeroDos;
        System.out.println("Introduce numero uno");
        numeroUno = lecturaTeclado.nextInt();

        System.out.println("Introduce numero dos");
        numeroDos = lecturaTeclado.nextInt();

        // El primero es par(C1) y(&&) el segundo impar (C2)
        boolean isParN1 = numeroUno % 2 == 0;
        boolean isImparN2 = numeroDos % 2 != 0;
        boolean comprobacion1 = isParN1 && isImparN2;
        //boolean comprobacion1 =  numeroUno % 2 == 0 && numeroDos % 2 != 0;

        //El primero es superior al doble del segundo (C3) e (&&) inferior a 8 (C4)
        boolean superiorPrimero = numeroUno > (numeroDos * 2);
        boolean inferiorPrimero = numeroUno < 8;
        boolean comprobacion2 = superiorPrimero && inferiorPrimero;

        //Son iguales (C5) o (||) la diferencia entre el primero y el segundo es menor que 2 (C6)
        boolean numerosIguales = numeroUno == numeroDos;
        boolean diferenciaNumeros = (numeroUno - numeroDos) < 2;
        boolean comprobacion3 = numerosIguales || diferenciaNumeros;

        System.out.println("El primero es par y el segundo impar "+comprobacion1);
        System.out.println("El primero es el doble del segundo y el primero es < 8 "+comprobacion2);
        System.out.println("Los numeros son iguales o su resta es < 2 "+comprobacion3);*/

        System.out.printf("%%");

    }
}
